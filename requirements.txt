flask
gunicorn
flask_sqlalchemy
flask_migrate
flask_login
python-dotenv

uvicorn[standard]
git add requirements.txt
git commit -m "Fix: add uvicorn[standard] to requirements"
git push
pip install uvicorn[standard] && uvicorn app.main:app --host=0.0.0.0 --port=10000
